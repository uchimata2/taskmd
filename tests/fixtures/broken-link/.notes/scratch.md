# Scratch

A [pointer](gone.md) to a file that is not there.

This lives in a dot-directory on purpose: `glob`'s `**` skips those,
which is how a broken link in the live handoff pointer stayed hidden.
