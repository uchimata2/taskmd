# The guide a migrated project keeps

The tasks moved to a backend. **These documents did not**, and they are what T-177 measured: a
project in this state keeps ordinary Markdown, and ordinary Markdown rots.

This link is dead on purpose: [the plan](plan.md) names a file this project does not have. Nothing
reported it before T-185, because the whole validator refused at the config and never reached a
document.

| Column | Column |
| :--- | :--- |
| a row that fits | and so does this |
