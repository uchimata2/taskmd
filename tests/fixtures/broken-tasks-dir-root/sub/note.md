A note one level down, holding a [dead link](./nowhere.md).

It is here to be *missed*: with `tasks_dir: .` every directory answers as a nested project, so this
file is never read and its dead link is never reported.
